{
  "description": "TypeScript versions of crypto and worker modules",
  "devDependencies": {
    "@types/node": "^26.0.0"
  },
  "name": "typescript-modules",
  "scripts": {
    "crypto": "bun run ./packages/crypto/main.ts",
    "crypto:bench": "bun run ./packages/crypto/utils/scrypt-benchmark.ts",
    "crypto:opts": "bun run ./packages/crypto/utils/scrypt-options.ts",
    "worker": "bun run ./packages/worker/main.ts",
    "quorum": "bun run ./packages/quorum/main.ts",
    "quorum:compile": "bun build --compile ./packages/quorum/main.ts --outfile quorum"
  },
  "version": "1.0.0"
}